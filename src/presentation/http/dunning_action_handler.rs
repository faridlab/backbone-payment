//! DunningAction REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc};
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{DunningActionService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreateDunningActionDto, UpdateDunningActionDto, PatchDunningActionDto, DunningActionResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum DunningActionError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for DunningActionError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for DunningActionError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "DUNNINGACTION_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "DUNNINGACTION_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "DUNNINGACTION_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "DUNNINGACTION_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for DunningAction.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /dunning_actions | List with pagination |
/// | POST | /dunning_actions | Create new |
/// | GET | /dunning_actions/:id | Get by ID |
/// | PUT | /dunning_actions/:id | Full update |
/// | PATCH | /dunning_actions/:id | Partial update |
/// | DELETE | /dunning_actions/:id | Soft delete |
/// | POST | /dunning_actions/bulk | Bulk create |
/// | POST | /dunning_actions/upsert | Upsert |
/// | GET | /dunning_actions/trash | List deleted |
/// | POST | /dunning_actions/:id/restore | Restore |
/// | DELETE | /dunning_actions/empty | Empty trash |
/// | GET | /dunning_actions/:id/deleted | Get deleted by ID |
/// | DELETE | /dunning_actions/trash/:id | Permanent delete from trash |
/// | GET | /dunning_actions/count | Count active entities |
/// | GET | /dunning_actions/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(DunningActionService::with_repository(repository));
/// let router = create_dunning_action_routes(service);
/// ```
pub fn create_dunning_action_routes(service: Arc<DunningActionService>) -> Router {
    BackboneCrudHandler::<DunningActionService, DunningAction, CreateDunningActionDto, UpdateDunningActionDto, DunningActionResponseDto>::routes(
        service,
        "/dunning_actions",
    )
}

/// Create Axum router with only the read (GET) endpoints for DunningAction.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_dunning_action_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_dunning_action_read_routes(service: Arc<DunningActionService>) -> Router {
    BackboneCrudHandler::<DunningActionService, DunningAction, CreateDunningActionDto, UpdateDunningActionDto, DunningActionResponseDto>::read_routes(
        service,
        "/dunning_actions",
    )
}

/// Create Axum router with only the write (mutation) endpoints for DunningAction.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_dunning_action_write_routes(service: Arc<DunningActionService>) -> Router {
    BackboneCrudHandler::<DunningActionService, DunningAction, CreateDunningActionDto, UpdateDunningActionDto, DunningActionResponseDto>::write_routes(
        service,
        "/dunning_actions",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_dunning_action_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<DunningActionService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_dunning_action_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

