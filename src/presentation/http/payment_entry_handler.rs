//! PaymentEntry REST handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Uses Axum and backbone-core's BackboneCrudHandler for all 12 CRUD endpoints.

use std::collections::HashMap;
use std::sync::Arc;

use axum::Router;
use serde::{Deserialize, Serialize};
use uuid::Uuid;
use chrono::{DateTime, Utc, NaiveDate};
use rust_decimal::Decimal;

// Backbone framework imports
use backbone_core::http::BackboneCrudHandler;

// Auth integration (optional)
#[cfg(feature = "auth")]
use backbone_auth::middleware::AuthContext;
#[cfg(feature = "auth")]
use backbone_auth::AuthMiddleware;

// Domain imports
use crate::domain::entity::*;
use crate::application::service::{PaymentEntryService, ServiceError};

// DTO imports
use crate::presentation::dto::{CreatePaymentEntryDto, UpdatePaymentEntryDto, PatchPaymentEntryDto, PaymentEntryResponseDto};


/// Application error type
#[derive(Debug, thiserror::Error)]
pub enum PaymentEntryError {
    #[error("Not found: {0}")]
    NotFound(String),
    #[error("Validation error: {0}")]
    Validation(String),
    #[error("Database error: {0}")]
    Database(String),
    #[error("Internal error: {0}")]
    Internal(String),
}

impl From<ServiceError> for PaymentEntryError {
    fn from(err: ServiceError) -> Self {
        match err {
            ServiceError::NotFound => Self::NotFound(err.to_string()),
            ServiceError::Validation(ref msg) => Self::Validation(msg.clone()),
            ServiceError::AlreadyExists(ref msg) => Self::Validation(msg.clone()),
            ServiceError::Repository(ref e) => Self::Database(e.to_string()),
            ServiceError::Internal(ref msg) => Self::Internal(msg.clone()),
        }
    }
}

impl axum::response::IntoResponse for PaymentEntryError {
    fn into_response(self) -> axum::response::Response {
        use axum::http::StatusCode;
        use axum::Json;

        let (status, code) = match &self {
            Self::NotFound(_) => (StatusCode::NOT_FOUND, "PAYMENTENTRY_NOT_FOUND"),
            Self::Validation(_) => (StatusCode::BAD_REQUEST, "PAYMENTENTRY_VALIDATION_ERROR"),
            Self::Database(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PAYMENTENTRY_DATABASE_ERROR"),
            Self::Internal(_) => (StatusCode::INTERNAL_SERVER_ERROR, "PAYMENTENTRY_INTERNAL_ERROR"),
        };

        let body = serde_json::json!({
            "success": false,
            "error": code,
            "message": self.to_string(),
        });

        (status, Json(body)).into_response()
    }
}

// =============================================================================
// Route Configuration
// =============================================================================

/// Create Axum router with all 16 Backbone endpoints for PaymentEntry.
///
/// # Routes
///
/// | Method | Path | Description |
/// |--------|------|-------------|
/// | GET | /payment_entries | List with pagination |
/// | POST | /payment_entries | Create new |
/// | GET | /payment_entries/:id | Get by ID |
/// | PUT | /payment_entries/:id | Full update |
/// | PATCH | /payment_entries/:id | Partial update |
/// | DELETE | /payment_entries/:id | Soft delete |
/// | POST | /payment_entries/bulk | Bulk create |
/// | POST | /payment_entries/upsert | Upsert |
/// | GET | /payment_entries/trash | List deleted |
/// | POST | /payment_entries/:id/restore | Restore |
/// | DELETE | /payment_entries/empty | Empty trash |
/// | GET | /payment_entries/:id/deleted | Get deleted by ID |
/// | DELETE | /payment_entries/trash/:id | Permanent delete from trash |
/// | GET | /payment_entries/count | Count active entities |
/// | GET | /payment_entries/trash/count | Count deleted entities |
///
/// # Example
///
/// ```text
/// let service = Arc::new(PaymentEntryService::with_repository(repository));
/// let router = create_payment_entry_routes(service);
/// ```
pub fn create_payment_entry_routes(service: Arc<PaymentEntryService>) -> Router {
    BackboneCrudHandler::<PaymentEntryService, PaymentEntry, CreatePaymentEntryDto, UpdatePaymentEntryDto, PaymentEntryResponseDto>::routes(
        service,
        "/payment_entries",
    )
}

/// Create Axum router with only the read (GET) endpoints for PaymentEntry.
///
/// Safe for public, unauthenticated exposure (e.g., reference data).
/// Mutations must be served separately via `create_payment_entry_write_routes`,
/// typically wrapped in an auth middleware layer.
pub fn create_payment_entry_read_routes(service: Arc<PaymentEntryService>) -> Router {
    BackboneCrudHandler::<PaymentEntryService, PaymentEntry, CreatePaymentEntryDto, UpdatePaymentEntryDto, PaymentEntryResponseDto>::read_routes(
        service,
        "/payment_entries",
    )
}

/// Create Axum router with only the write (mutation) endpoints for PaymentEntry.
///
/// These routes must NOT be publicly exposed. Wrap them with an auth
/// middleware before nesting into the application router.
pub fn create_payment_entry_write_routes(service: Arc<PaymentEntryService>) -> Router {
    BackboneCrudHandler::<PaymentEntryService, PaymentEntry, CreatePaymentEntryDto, UpdatePaymentEntryDto, PaymentEntryResponseDto>::write_routes(
        service,
        "/payment_entries",
    )
}

/// Create authenticated routes with auth middleware.
///
/// Requires the `auth` feature flag. The `AuthMiddleware` implementation
/// is responsible for extracting and validating tokens, then providing
/// an `AuthContext` via request extensions.
#[cfg(feature = "auth")]
pub fn create_protected_payment_entry_routes<A: AuthMiddleware + Send + Sync + 'static>(
    service: Arc<PaymentEntryService>,
    auth: Arc<A>,
) -> Router {
    use axum::middleware;
    use axum::response::IntoResponse;

    let auth_layer = auth.clone();
    create_payment_entry_routes(service)
        .layer(middleware::from_fn(move |mut req: axum::extract::Request, next: axum::middleware::Next| {
            let auth = auth_layer.clone();
            async move {
                let token = req.headers()
                    .get(axum::http::header::AUTHORIZATION)
                    .and_then(|h| h.to_str().ok())
                    .and_then(|raw| raw.strip_prefix("Bearer ").or_else(|| raw.strip_prefix("bearer ")))
                    .unwrap_or("");
                match auth.authenticate(token).await {
                    Ok(ctx) => {
                        req.extensions_mut().insert(ctx);
                        next.run(req).await
                    }
                    Err(_) => {
                        (axum::http::StatusCode::UNAUTHORIZED,
                         axum::Json(serde_json::json!({
                             "success": false,
                             "error": "unauthorized",
                             "message": "Authentication required"
                         }))
                        ).into_response()
                    }
                }
            }
        }))
}

