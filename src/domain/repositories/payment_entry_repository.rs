//! Repository trait for PaymentEntry aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the PaymentEntry aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::{PaymentEntry, GlPostingState, PaymentPartyType, PaymentStatus, PaymentType};

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentEntryPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl PaymentEntryPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct PaymentEntryPaginatedResult {
    pub data: Vec<PaymentEntry>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct PaymentEntryFilter {
    pub payment_number: Option<String>,
    pub company_id: Option<Uuid>,
    pub branch_id: Option<Uuid>,
    pub payment_type: Option<PaymentType>,
    pub party_type: Option<PaymentPartyType>,
    pub party_id: Option<Uuid>,
    pub currency: Option<String>,
    pub mode_of_payment_id: Option<Uuid>,
    pub bank_account_id: Option<Uuid>,
    pub party_account_id: Option<Uuid>,
    pub status: Option<PaymentStatus>,
    pub posting_state: Option<GlPostingState>,
    pub journal_id: Option<Uuid>,
    pub accounting_post_id: Option<Uuid>,
    pub reference_no: Option<String>,
    pub notes: Option<String>,
}

impl PaymentEntryFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.payment_number.is_some() || self.company_id.is_some() || self.branch_id.is_some() || self.payment_type.is_some() || self.party_type.is_some() || self.party_id.is_some() || self.currency.is_some() || self.mode_of_payment_id.is_some() || self.bank_account_id.is_some() || self.party_account_id.is_some() || self.status.is_some() || self.posting_state.is_some() || self.journal_id.is_some() || self.accounting_post_id.is_some() || self.reference_no.is_some() || self.notes.is_some()
    }
}

/// Repository trait for PaymentEntry aggregate
///
/// This trait defines the persistence contract for PaymentEntry entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait PaymentEntryRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new payment_entry entity
    async fn save(&self, entity: &PaymentEntry) -> Result<PaymentEntry>;

    /// Find payment_entry by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<PaymentEntry>>;

    /// Find all payment_entry entities
    async fn find_all(&self) -> Result<Vec<PaymentEntry>>;

    /// Update payment_entry by ID
    async fn update(&self, id: &str, entity: &PaymentEntry) -> Result<Option<PaymentEntry>>;

    /// Delete payment_entry by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List payment_entry with pagination
    async fn list(&self, params: PaymentEntryPaginationParams) -> Result<PaymentEntryPaginatedResult>;

    /// List payment_entry with pagination and filters
    async fn list_with_filters(&self, params: PaymentEntryPaginationParams, filters: PaymentEntryFilter) -> Result<PaymentEntryPaginatedResult>;

    /// Count all payment_entry entities
    async fn count(&self) -> Result<u64>;

    /// Count payment_entry entities matching filters
    async fn count_with_filters(&self, filters: PaymentEntryFilter) -> Result<u64>;

    /// Check if payment_entry exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete payment_entry by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted payment_entry by ID
    async fn restore(&self, id: &str) -> Result<Option<PaymentEntry>>;

    /// List soft-deleted payment_entry entities
    async fn list_deleted(&self, params: PaymentEntryPaginationParams) -> Result<PaymentEntryPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save payment_entry entities
    async fn bulk_save(&self, entities: &[PaymentEntry]) -> Result<Vec<PaymentEntry>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
