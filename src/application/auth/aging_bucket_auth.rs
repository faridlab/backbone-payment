//! Auth configuration for AgingBucket handlers
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Implements `ResourcePolicy<AgingBucket>` with entity-specific permission
//! string constants. Guard middleware logic lives in backbone-auth.

use backbone_auth::{ResourcePolicy, ResourceAction, PermissionGuard, ServicePermissionGuard};
use async_trait::async_trait;

use crate::application::service::AgingBucketService;
use crate::domain::entity::AgingBucket;

/// Permission string constants for AgingBucket operations.
pub mod permissions {
    pub const LIST:        &str = "aging_bucket:list";
    pub const READ:        &str = "aging_bucket:read";
    pub const CREATE:      &str = "aging_bucket:create";
    pub const UPDATE:      &str = "aging_bucket:update";
    pub const DELETE:      &str = "aging_bucket:delete";
    pub const RESTORE:     &str = "aging_bucket:restore";
    pub const TRASH:       &str = "aging_bucket:trash";
    pub const EMPTY_TRASH: &str = "aging_bucket:empty_trash";
    pub const BULK_CREATE: &str = "aging_bucket:bulk_create";
    pub const UPSERT:      &str = "aging_bucket:upsert";
}

/// Resource policy for AgingBucket — maps CRUD actions to permission strings.
pub struct AgingBucketPolicy;

#[async_trait]
impl ResourcePolicy<AgingBucket> for AgingBucketPolicy {
    async fn can(
        &self,
        action: ResourceAction,
        _entity: &AgingBucket,
        ctx: &backbone_auth::middleware::AuthContext,
    ) -> bool {
        let required = match action {
            ResourceAction::Read | ResourceAction::List => permissions::READ,
            ResourceAction::Create => permissions::CREATE,
            ResourceAction::Update | ResourceAction::Patch => permissions::UPDATE,
            ResourceAction::Delete | ResourceAction::HardDelete => permissions::DELETE,
            ResourceAction::Restore => permissions::RESTORE,
            ResourceAction::Custom(_) => return false,
        };
        ctx.permissions.iter().any(|p| p == required)
    }
}

/// Permission guard for AgingBucket handlers.
///
/// Wraps `AgingBucketService` and checks `AgingBucketPolicy` before delegating.
pub type AgingBucketGuard = PermissionGuard<AgingBucket>;

/// Service-integrated permission guard for AgingBucket (for use in generated handlers).
pub type AgingBucketServiceGuard = ServicePermissionGuard<AgingBucket, AgingBucketService, AgingBucketPolicy>;

// <<< CUSTOM
// END CUSTOM
