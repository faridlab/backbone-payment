//! PaymentEntry seeder
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Seeds initial PaymentEntry data into the database.

use anyhow::Result;
use async_trait::async_trait;
use sqlx::PgPool;

use super::Seeder;

// ============================================================================
// PAYMENTENTRY SEEDER
// ============================================================================

/// Seeder for payment_entry
pub struct SeedPaymentEntrySeeder;

impl SeedPaymentEntrySeeder {
    pub fn new() -> Self {
        Self
    }
}

impl Default for SeedPaymentEntrySeeder {
    fn default() -> Self {
        Self::new()
    }
}

#[async_trait]
impl Seeder for SeedPaymentEntrySeeder {
    fn name(&self) -> &'static str {
        "SeedPaymentEntrySeeder"
    }

    fn order(&self) -> i32 {
        2
    }

    async fn should_run(&self, pool: &PgPool) -> Result<bool> {
        // Check if payment.payment_entries table has any data
        let count: (i64,) = sqlx::query_as("SELECT COUNT(*) FROM payment.payment_entries")
            .fetch_one(pool)
            .await?;
        Ok(count.0 == 0)
    }

    async fn run(&self, pool: &PgPool) -> Result<()> {
        // Run the SQL seed file
        let sql = include_str!("../../migrations/seeds/payment_entry_seed.sql");
        sqlx::raw_sql(sql).execute(pool).await?;
        Ok(())
    }

    async fn rollback(&self, pool: &PgPool) -> Result<()> {
        sqlx::query("DELETE FROM payment.payment_entries")
            .execute(pool)
            .await?;
        Ok(())
    }
}

// ============================================================================
// CUSTOM SEED DATA
// ============================================================================

// <<< CUSTOM SEED DATA START >>>
// Add custom seed data methods here
// <<< CUSTOM SEED DATA END >>>

